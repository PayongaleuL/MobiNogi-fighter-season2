# 시즌2 룬 데이터·DPS 작업 지침

1. `results/260814_룬설명목록.md`는 사용자가 검수한 88개 시즌2 룬의 설명 원본이며, 설명 문구를 임의로 고치지 않는다.
2. `src/data/runes.json`의 수동 보정 스탯·가동률·조건부 효과는 룬 파서 자동 추출값보다 우선한다. 룬 교정실은 기존 항목을 자동 초기화하거나 덮어쓰지 않고 누락 룬만 동기화한다.
3. 상시 `공격력%`는 마을 공격력에 한 번만 가산하며 가동률·초월 배율을 적용하지 않는다. 조건부 공격력은 `조건부공증%`와 효과별 가동률로 모델링한다.
4. `흐릿한 형상`의 기본 가동률은 70%, `타오르는 영광`의 불씨름 12중첩 궁극기 효과 기본 가동률은 13%다.
5. 룬·세공·교정실·계산 엔진을 수정하면 `npm run lint && npm run test:coverage && npm run build`와 `node scripts/compare_latest_reference_presets.mjs`를 모두 통과해야 한다. 커버리지 임계값은 Statements, Branches, Functions, Lines 각각 95%다.
6. 모든 룬 선택은 유한 DPS를 계산해야 하며, 새 조건부 효과는 효과 ID·기본 가동률·원문 근거·회귀 테스트를 함께 추가한다.
